package com.group.application;

import com.group.application.config.AppConfig;
import org.springframework.context.ApplicationContext;
import org.springframework.context.annotation.AnnotationConfigApplicationContext;

import java.io.IOException;
import java.sql.SQLException;

public class SeleniumTest {
/*
    public static void main(String[] args) throws IOException, SQLException, CloneNotSupportedException {

        ApplicationContext context = new AnnotationConfigApplicationContext(AppConfig.class);

        RunOrLoad rol = context.getBean(RunOrLoad.class);

        Db db = Db.getInstance();

        System.setProperty("webdriver.chrome.driver", "./chromedriver");
        WebDriver driver = new ChromeDriver();
        driver.manage().window().maximize();

        //rol.run(context, res);

        //rol.loadNames(driver);

        //driver.close();
    }
*/}
